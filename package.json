{
  "scripts": {
    "build": "npm run build --prefix frontend && mv ./frontend/build ./backend/build",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-plugin-react": "^7.34.1"
  }
}
